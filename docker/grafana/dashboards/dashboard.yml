# Grafana 看板 provisioning：从本目录加载所有 json 看板。
apiVersion: 1

providers:
  - name: TravelPal
    orgId: 1
    folder: TravelPal
    type: file
    disableDeletion: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards
